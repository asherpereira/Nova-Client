# Contributing

Keep commits focused. Keep UI independent from protocol/networking. Build and test before pull requests. Document contract changes. Never commit credentials, signing keys, production configuration or local machine state.
